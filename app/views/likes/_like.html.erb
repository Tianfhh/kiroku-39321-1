<% if user_signed_in? && @entry.liked_by?(current_user)%>
    <%=link_to "いいね済", entry_likes_path(@entry.id),  method: :delete, class: "entry-likes1", remote: true %>
<% else %>
    <%=link_to "いいね", entry_likes_path(@entry.id), method: :post, class: "entry-likes2", remote: true %>
<% end %>