<% if user_signed_in? && @entry.liked_by?(current_user)%>
    <%=link_to "お気に入り済", entry_likes_path(@entry.id),  method: :delete, class: "entry_likes", remote: true %>
<% else %>
    <%=link_to "お気に入り", entry_likes_path(@entry.id), method: :post, class: "entry_likes", remote: true %>
<% end %>